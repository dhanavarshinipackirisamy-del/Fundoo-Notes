spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fundoo_note
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    spring:
      data:
        redis:
          host: localhost
          port: 6379